# DaemonSU
DaemonSU is a lightweight KernelSU module enabling Telegram-based remote control of rooted Android devices. Running as a persistent background daemon, it executes system-level commands with minimal resource usage.
